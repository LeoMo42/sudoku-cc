# Sudoku Game

Полнофункциональная игра Sudoku, созданная на React + Vite с использованием Tailwind CSS.

## Возможности

✅ **Генерация головоломок** - Создание новых валидных головоломок Sudoku
✅ **Проверка решения** - Автоматическая проверка правильности решения
✅ **Система подсказок** - 3-5 подсказок на игру в зависимости от сложности
✅ **Таймер** - Отслеживание времени прохождения
✅ **4 уровня сложности**:
  - **Легкий**: 40-50 заполненных ячеек, 5 подсказок
  - **Средний**: 30-40 заполненных ячеек, 4 подсказки
  - **Сложный**: 25-30 заполненных ячеек, 3 подсказки
  - **Эксперт**: 20-25 заполненных ячеек, 3 подсказки

✅ **Режим заметок** - Возможность делать пометки в ячейках
✅ **Автосохранение** - Игра автоматически сохраняется в localStorage
✅ **Клавиатурная навигация**:
  - `1-9` - ввод чисел
  - `Стрелки` - навигация по полю
  - `Backspace/Delete` - очистка ячейки
  - `N` - переключение режима заметок

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Preview production build
npm run preview

# Запуск Storybook (компоненты)
npm run storybook

# Сборка Storybook
npm run build-storybook

# Запуск тестов
npm run test         # Watch mode
npm run test:run     # Run once
npm run test:ui      # UI interface
```

## Технологический стек

- **React** - UI библиотека
- **Vite** - Build tool
- **Tailwind CSS** - Стилизация
- **Context API + useReducer** - State management
- **Storybook** - Документация компонентов
- **Vitest** - Тестирование

## Архитектура проекта

```
src/
├── components/
│   ├── Board/           # Игровое поле и ячейки
│   │   ├── Board.jsx
│   │   └── Cell.jsx
│   ├── Controls/        # Элементы управления
│   │   ├── DifficultySelector.jsx
│   │   ├── GameControls.jsx
│   │   ├── NumberPad.jsx
│   │   └── Timer.jsx
│   ├── Game/           # Главный контейнер
│   │   └── GameContainer.jsx
│   └── UI/             # Базовые UI компоненты
│       ├── Button.jsx
│       └── Modal.jsx
├── context/            # State management
│   └── GameContext.jsx
├── hooks/              # Кастомные хуки
│   ├── useGameState.js
│   └── useTimer.js
├── utils/              # Алгоритмы и утилиты
│   ├── constants.js
│   ├── sudokuGenerator.js
│   ├── sudokuSolver.js
│   └── sudokuValidator.js
├── App.jsx
├── main.jsx
└── index.css
```

## Алгоритмы

### Генерация головоломок
1. Создание полностью заполненной валидной доски (заполнение диагональных блоков + backtracking)
2. Удаление чисел с проверкой единственности решения

### Решение головоломок
Использует рекурсивный backtracking алгоритм для поиска решения

### Валидация
Проверка корректности хода с учетом правил Sudoku (строка, столбец, блок 3x3)

## Storybook - Документация компонентов

Проект включает Storybook для разработки и документирования компонентов в изоляции.

### Запуск Storybook:
```bash
npm run storybook
```

Откроется http://localhost:6006/ (или 6007, если 6006 занят)

### Доступные stories:

**UI компоненты:**
- Button - Кнопки с вариантами (primary, secondary, success, danger)
- Modal - Модальные окна

**Board компоненты:**
- Cell - Отдельная ячейка судоку (с различными состояниями)
- Board - Игровое поле 9x9

**Controls:**
- Timer - Отображение времени
- NumberPad - Панель ввода чисел
- DifficultySelector - Выбор уровня сложности
- GameControls - Кнопки управления игрой

Каждая story включает:
- Интерактивные контролы для изменения props
- Несколько вариантов компонента в разных состояниях
- Автодокументацию

## Производительность

- **React.memo** для Cell компонента (оптимизация 81 экземпляра)
- **useCallback** для обработчиков событий
- Кастомная функция сравнения для мемоизации Cell

## Лицензия

MIT
