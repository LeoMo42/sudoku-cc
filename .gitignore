# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — never track. .env.example IS allowed (template for new
# developers / docs). Without these rules, the moment someone adds .env
# with VITE_ANALYTICS_KEY / VITE_SENTRY_DSN / dev API tokens it leaks
# to the public repo on the next `git add`.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

__pycache__
*.pyc

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.gstack/
.claude/

# Review/QA artifacts (gstack skills write screenshots here)
screenshots/
